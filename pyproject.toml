[build-system]
requires = ['setuptools', 'wheel', 'versioneer[toml]']

[tool.versioneer]
VCS = "git"
style = "pep440"
versionfile_source = "ch_util/_version.py"
versionfile_build = "ch_util/_version.py"
tag_prefix = "v"
parentdir_prefix = "ch_util-"
